parameters:
  image: ''
  configuration: ''
  platform: ''
  steps: []

- job:

  displayName: '${{ image }} (${{ configuration }}) (${{ platform }})'
  pool:
    vmImage: ${{ image }}

  steps:
    - ${{ each step in parameters.steps }}:
      - ${{ step }}
